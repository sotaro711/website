export const meta = {
  title: "タイトル",
  date: "2026-08-21",
  description: "検索結果などに出る1行の説明。無ければ省略してよい",
};

新しい記事を書くときはこのファイルをコピーして、`content/posts/記事のスラッグ.mdx` として保存する（ファイル名がそのままURLになる）。

`_` で始まるファイルは公開されないので、下書きは `_draft-xxx.mdx` のような名前で置いておける。このテンプレート自体は消さないこと（.mdx が1つも無いとビルドが通らないため）。
